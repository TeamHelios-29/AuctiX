import { VerificationForm } from '@/components/organisms/VerificationForm';

export default function SellerVerificationSubmitPage() {
  return (
    <div className="flex flex-col items-center justify-center h-screen w-full">
      <VerificationForm />
    </div>
  );
}
